import React, { Fragment } from 'react'

const index = (props) => {
    return (
        <Fragment>
            <h1>Isto é o home!!{props.title}</h1>
        </Fragment>
    )
}

export default index
